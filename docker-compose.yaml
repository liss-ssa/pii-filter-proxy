services:
  pii-api:
    build: .
    env_file: .env
    ports: ["8000:8000"]
    read_only: true
    tmpfs: ["/tmp:size=64m,noexec,nosuid"]
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    pids_limit: 128
    mem_limit: 2g
    cpus: 2.0
    restart: unless-stopped
